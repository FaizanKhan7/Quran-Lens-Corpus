import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Quran Lens — Next.js 16.2.1 + Turbopack */
  // Note: PWA service worker is a plain public/sw.js; no webpack plugin needed.
};

export default nextConfig;
